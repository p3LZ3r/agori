<%= simple_form_for(@user) do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :merchant, as: :boolean %>
  <%= f.input :farmer, as: :boolean %>
  <%= f.button :submit %>
<% end %>
